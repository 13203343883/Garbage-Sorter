
---

把这段复制到你的文档里，然后发给队友。他们会知道每天该干什么、怎么拉代码、怎么提交。有问题随时叫我。👍
---
用户输入了敏感信息，根据相关政策，我们无法处理此类内容。好的，我把**文档写完**并加上了你要求的这一页，你现在可以直接把下面**一整份完整文档**复制到 `README.md` 里，提交到 GitHub 根目录。

---

```markdown
# 项目概要说明书

## 一、项目概述

**项目名称**：基于YOLO与微信小程序的智能垃圾分类识别系统

**开发周期**：10天（冲刺模式）

**项目目标**：开发一个完整可演示的垃圾分类识别系统，用户通过微信小程序拍照，系统调用后端模型进行识别并返回分类结果。模型部分要求自主训练优化，满足老师“不单纯调用API”的要求。


## 二、团队成员与分工

| 角色 | 成员 | 核心职责 | 主要产出物 |
|------|------|----------|------------|
| **A同学（算法）** | 待定 | YOLO模型训练、调优、对比实验、导出ONNX模型 | 训练脚本、实验对比数据、ONNX模型文件 |
| **B同学（后端）** | 本人 | FastAPI接口开发、数据库设计、模型服务部署、接口文档维护 | `/health`接口、`/predict`接口、数据库表结构、`API.md` |
| **C同学（前端）** | 待定 | 微信小程序界面开发、拍照交互、结果展示与历史记录 | 拍照页、结果展示页、历史记录页 |


## 三、技术栈

| 层级 | 技术选型 |
|------|----------|
| **算法** | YOLOv5/v8 + PyTorch + ONNX |
| **后端** | Python 3.x + FastAPI + uvicorn |
| **数据库** | SQLite（开发）/ MySQL（备选） |
| **模型推理** | onnxruntime + OpenCV + NumPy |
| **前端** | 微信小程序原生开发 |
| **版本控制** | Git + GitHub |

### 分支策略

| 分支 | 说明 |
|------|------|
| `main` | 稳定版本，仅合并已完成的功能 |
| `dev-backend` | 后端开发分支（B同学） |
| `dev-algorithm` | 算法开发分支（A同学） |
| `dev-miniprogram` | 前端开发分支（C同学） |


## 四、核心功能模块

| 模块 | 说明 | 负责人 |
|------|------|--------|
| **模型训练** | 下载数据集，YOLO训练/调优，导出ONNX | A同学 |
| **模型部署** | onnxruntime加载，封装为`/predict`接口 | B同学 |
| **数据库** | 用户表、识别记录表、垃圾类别表 | B同学 |
| **小程序拍照** | 调用相机拍照/相册选择 | C同学 |
| **结果展示** | 展示垃圾名称、类别、置信度 | C同学 |


## 五、当前进度

| 模块 | 状态 | 备注 |
|------|------|------|
| GitHub仓库创建与初始化 | ✅ 已完成 | 包含`backend/`和`miniprogram/`目录 |
| 后端虚拟环境与依赖 | ✅ 已完成 | FastAPI + onnxruntime + OpenCV等 |
| `/health`健康检查接口 | ✅ 已完成 | 访问`/health`返回`{"status":"ok"}` |
| 后端代码合并至`main` | ✅ 已完成 | `dev-backend`已合入`main` |
| `.gitignore`配置 | ✅ 已完成 | 已忽略`venv/`、模型文件、缓存等 |
| 换行符警告处理 | ✅ 已完成 | `git config core.autocrlf true` |
| 分支推送与远程关联 | ✅ 已完成 | `dev-backend`已关联远程 |
| 模型训练与导出ONNX | ⏳ 等待A同学 | 需提供可用的ONNX文件 |
| 微信小程序框架 | ⏳ 等待C同学 | 需完成拍照页和结果页 |


## 六、下一步计划

| 任务 | 负责人 | 截止时间 |
|------|--------|----------|
| 下载垃圾分类数据集，跑通YOLO训练脚本 | A同学 | 第3天 |
| 设计数据库表结构并建表 | B同学 | 第2天 |
| 创建`/predict`接口骨架（先返回模拟数据） | B同学 | 第3天 |
| 搭建小程序页面框架（拍照页 + 结果展示页） | C同学 | 第3天 |
| 完善接口文档`API.md` | B同学 | 第2天 |


## 七、协作规范

### Git操作规范

1. **每天开工**：`git pull origin dev-xxx` 拉取最新代码
2. **每天收工**：`git add .` → `git commit -m "描述"` → `git push`
3. **各自只修改自己文件夹内的内容**：
   - A同学：`backend/` 下与训练相关的部分
   - B同学：`backend/` 下与后端服务相关的部分
   - C同学：`miniprogram/` 下所有文件
4. **合并到`main`的时机**：完成一个独立功能点后，切到`main`执行`git merge dev-xxx`

### 沟通规范

- 修改公共文件（如`requirements.txt`）前在群里告知
- 遇到Git冲突及时截图发群，不要强行操作
- 每天同步一次进度，确保不偏离方向


## 八、备注

- ONNX模型文件若超过100MB，不上传GitHub，通过百度网盘共享
- 答辩前2天完成最终合并和完整测试
- 答辩当天准备本地环境兜底（防止网络问题影响演示）


## 九、Git协作规范（队友必读）

### 1. 克隆仓库（只做一次）

```bash
git clone https://github.com/13203343883/Garbage-Sorter.git
cd Garbage-Sorter